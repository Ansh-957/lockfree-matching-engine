# bench/CMakeLists.txt
#
# Benchmarks are built from the top-level CMakeLists.txt.
# Each benchmark is its own executable target defined there.
# This file exists only as documentation / placeholder.
