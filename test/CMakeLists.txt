# test/CMakeLists.txt
#
# Tests are built from the top-level CMakeLists.txt.
# All test sources are compiled into the `unit_tests` target there.
# This file exists only as documentation / placeholder.
